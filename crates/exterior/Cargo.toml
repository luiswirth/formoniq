[package]
name = "exterior"
version = "0.2.0"
edition = "2021"
license = "MIT OR Apache-2.0"

[dependencies]
gramian = { path = "../gramian" }
multiindex = { path = "../multiindex" }

nalgebra = "0.35.0"

[dev-dependencies]
approx = "0.5.1"
