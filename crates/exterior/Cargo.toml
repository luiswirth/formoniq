[package]
name = "exterior"
version = "0.2.0"
edition = "2021"
license = "MIT OR Apache-2.0"

[dependencies]
common = { path = "../common" }

nalgebra = "0.35.0"

itertools = "0.15.0"
approx = "0.5.1"
