[package]
name = "multiindex"
version = "0.2.0"
edition = "2021"
license = "MIT OR Apache-2.0"

[dependencies]
formoniq-linalg = { path = "../linalg" }
num-integer = "0.1.46"

[dev-dependencies]
itertools = "0.15.0"
