[package]
name = "gramian"
version = "0.2.0"
edition = "2021"
license = "MIT OR Apache-2.0"

[dependencies]
formoniq-linalg = { path = "../linalg" }

serde = { version = "1.0.228", features = ["derive"] }
