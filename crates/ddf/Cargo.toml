[package]
name = "ddf"
version = "0.2.0"
edition = "2021"

[dependencies]
exterior = { path = "../exterior" }
manifold = { path = "../manifold" }
common = { path = "../common" }

nalgebra = "0.33.2"
nalgebra-sparse = "0.10.0"

approx = "0.5.1"
