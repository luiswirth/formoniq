[package]
name = "whitney"
version = "0.1.0"
edition = "2021"

[dependencies]
exterior = { path = "../exterior" }
manifold = { path = "../manifold" }
multi-index = { path = "../multi-index" }
common = { path = "../common" }
