[package]
name = "formoniq-linalg"
version = "0.2.0"
edition = "2021"
license = "MIT OR Apache-2.0"

[dependencies]
nalgebra = { version = "0.35.0", features = ["serde-serialize"] }
nalgebra-sparse = "0.12.0"
faer = "0.24.4"

num-traits = "0.2.19"
